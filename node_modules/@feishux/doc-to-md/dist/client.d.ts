import { FlowUsToMarkdownOptions } from './types';
import { IBlock } from '@feishux/shared';
/**
 * FlowUs文档转Md
 */
export declare class FeiShuToMarkdown {
    private readonly feiShuClient?;
    constructor(options?: FlowUsToMarkdownOptions);
    pageToMarkdown(id: string): Promise<string>;
    toMarkdownString(blocks: IBlock[]): string;
}
