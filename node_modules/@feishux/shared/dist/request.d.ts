import { HttpClientResponse, RequestOptions } from 'urllib';
/**
 * 网络请求封装
 * @param url
 * @param reqOpts
 */
export declare const request: <T>(url: string, reqOpts?: RequestOptions) => Promise<HttpClientResponse<{
    code: number;
    data: T;
}>>;
