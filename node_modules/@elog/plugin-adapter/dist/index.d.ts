import { DocDetail } from '@elog/types';

/**
 * hexo 文章生产适配器
 *
 * @param {DocDetail} post 文章
 * @return {String} text
 */
declare function matterMarkdownAdapter(post: DocDetail): string;

declare function markdownAdapter(post: DocDetail): string;

/**
 * 将markdown转wiki
 * @param post
 */
declare function wikiAdapter(post: DocDetail): string;

/**
 * markdown转html（代码高亮）
 * @param post
 */
declare function htmlAdapterWithHighlight(post: DocDetail): string;
/**
 * markdown转html（无代码高亮）
 * @param post
 */
declare function htmlAdapter(post: DocDetail): string;

export { htmlAdapter, htmlAdapterWithHighlight, markdownAdapter, matterMarkdownAdapter, wikiAdapter };
